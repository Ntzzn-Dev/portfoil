import img0 from './SnakeImg.png';

const imgs = [img0];
const desc = "Este é o clássico Jogo da Cobra, desenvolvido para ser jogado diretamente no navegador. Refeito usando apenas HTML, CSS e JavaScript Puro";

export { img0, imgs, desc };